module.exports = {
  semi: true, // ставить крапки з комою
  singleQuote: true, // одинарні лапки
  trailingComma: 'es5', // кома після об'єктів/масивів
  tabWidth: 2, // 2 пробіли на відступ
  printWidth: 80, // перенос рядків після 80 символів
  bracketSpacing: true, // { foo: bar } замість {foo:bar}
  arrowParens: 'avoid', // (x) => ... → x => ...
};
